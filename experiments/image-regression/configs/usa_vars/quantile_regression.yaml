uq_method:
  _target_: uq_method_box.uq_methods.QuantileRegressionModel
  model:
    _target_: timm.create_model # timm.create_model for timm models
    # all arguments to initiate the mlp for example
    model_name: "resnet18"
    num_classes: 3
    in_chans: 4
    pretrained: True
  optimizer:
    _target_: torch.optim.Adam # can change optimizers here easily
    _partial_: true
    lr: 0.001
    weight_decay: 0.001